#include "shell.h"

/**
 * parse - Trim whitespace from the given command.
 * @command: The command to be parsed and trimmed.
 *
 * Description:
 * This function trims leading and trailing whitespaces from the command.
 */

void parse(char *command)
{
	trim(command);
}
